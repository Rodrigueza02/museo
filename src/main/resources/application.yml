# Archivo YAML con propiedades adicionales de configuración
spring:
  datasource:
    url: jdbc:postgresql://db.uerkoweysjcsflunegui.supabase.co:5432/museovivo
    username: postgres
    password: Malua.230225
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${PORT:8080}

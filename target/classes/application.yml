spring:
  application:
    name: museovivo-backend

  datasource:
    url: jdbc:postgresql://aws-1-us-east-2.pooler.supabase.com:5432/postgres?pgbouncer=true&prepareThreshold=0&sslmode=disable&tcpKeepAlive=true
    username: postgres.uerkoweysjcsflunegui
    password: Malua.230225
    driver-class-name: org.postgresql.Driver

    hikari:
      maximum-pool-size: 1
      minimum-idle: 1
      connection-timeout: 30000
      idle-timeout: 30000
      max-lifetime: 30000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    open-in-view: false

server:
  port: ${PORT:8080}

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    com.museum.backend: DEBUG
